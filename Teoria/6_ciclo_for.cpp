// Ciclo (bucle for)
#include<stdio.h>
#include<conio.h>
int main()
{
	int i;
	// en el ciclo for tenemos: condición de inicio, condición de para e incremento
	for(i=1;i<11;i++) //for(desde_donde;hasta_donde;incremento)
		printf("%i ",i);
	getch();
	return 0;
}
